import type { ReadingPost } from '@/lib/types';

// Add book entries here. Each entry creates a card on /blog/books
// and a detail page at /blog/books/[slug].
export const books: ReadingPost[] = [];
